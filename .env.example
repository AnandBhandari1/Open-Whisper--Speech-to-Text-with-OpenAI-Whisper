# FastSimple Environment Variables
# Copy this file to .env and customize as needed

# Force CPU mode (ignore GPU even if available)
# FORCE_CPU=0

# Disable grammar correction (already disabled in code, but kept for reference)
# DISABLE_GRAMMAR=1

# Display server type (auto-detected, but can be forced)
# XDG_SESSION_TYPE=wayland
# XDG_SESSION_TYPE=x11

# Custom hotkey (requires code modification)
# Currently hardcoded to F8

# Model download cache location (optional)
# XDG_CACHE_HOME=/path/to/cache
